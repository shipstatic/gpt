{
  "$comment": "The review team runs these against the live plugin, so every positive case must reproducibly succeed and every negative case must reproducibly NOT invoke the plugin. Exactly five positive and three negative, the portal's minimum. Every positive case is ANONYMOUS by design: OpenAI asks for demo credentials only if the server requires sign-in, and this one does not, so no case needs an account, a fixture or a password (the /gpt deploy takes none). `tools_triggered` is the exact MCP tool name. Descriptions and expected outputs are review-facing: what the reviewer sees, never how the server does it.",
  "positive": [
    {
      "description": "Single-file deploy, the simplest path. The model writes one index.html and deploys it with no account, no confirmation gate and no setup; the deploy card renders inline. The screenshot tile may read \"Preview loading\" for up to about thirty seconds while the capture lands.",
      "user_prompt": "Deploy a single-page website that says \"Hello world\" in big bold text.",
      "tools_triggered": "deployments_upload",
      "expected_output": "A deploy card with a live *.shipstatic.com URL, \"1 file\" and a small byte count, a screenshot of the page, and a \"Claim to keep forever\" link. Opening the URL shows the Hello world page."
    },
    {
      "description": "Multi-file site with cross-links. The model generates index.html, styles.css and about.html and sends all three in ONE deploy call; the stylesheet applies and the about link works on the live site.",
      "user_prompt": "Build me a landing page for a fictional coffee shop called \"Blackbird Coffee\" with an index.html, a styles.css file, and a small about.html page. Deploy it.",
      "tools_triggered": "deployments_upload",
      "expected_output": "A deploy card showing \"3 files\". The URL opens a styled landing page and its About link opens /about.html."
    },
    {
      "description": "Deterministic content the reviewer can verify with a single fetch of the URL. The model must use the exact heading and title text and must echo the deployment URL in its reply.",
      "user_prompt": "Deploy an HTML page whose <h1> reads exactly \"Hello reviewer\" and whose <title> reads \"ShipStatic test\". Then tell me the URL.",
      "tools_triggered": "deployments_upload",
      "expected_output": "A deploy card and the URL repeated in the reply. Fetching the URL returns HTML containing exactly <h1>Hello reviewer</h1> and <title>ShipStatic test</title>."
    },
    {
      "description": "Labels on a deploy. The model passes the two labels with the deploy call (labels are lowercase, 3 to 25 characters); the result lists them and the reply names them. No account is needed.",
      "user_prompt": "Deploy a one-page \"coming soon\" placeholder and label the deployment \"draft\" and \"v1\".",
      "tools_triggered": "deployments_upload",
      "expected_output": "A deploy card for the placeholder page, and the reply confirms the deployment carries the labels draft and v1 (the tool result lists both under labels)."
    },
    {
      "description": "The anonymous lifetime and the claim link, explained. Without an account a deployment expires in 3 days unless claimed; the tool result carries a one-time claim URL and the server's instructions tell the model to show it. The reviewer sees the model relay both facts correctly.",
      "user_prompt": "Deploy a page that says \"Keep me\" and explain how I can keep this site permanently.",
      "tools_triggered": "deployments_upload",
      "expected_output": "A deploy card with the live URL and the claim link, and a reply that says the site expires in 3 days unless claimed and that opening the claim link (creating a free account if needed) keeps it permanently."
    }
  ],
  "negative": [
    {
      "description": "An unrelated productivity request. Nothing about publishing a site; the plugin must not be invoked.",
      "user_prompt": "What meetings do I have tomorrow?",
      "expected_output": "ChatGPT answers from its own tools or says it cannot see a calendar. ShipStatic is not called and no deploy card appears."
    },
    {
      "description": "A nearby question that needs an explanation, not a deployment. The plugin's domain tools return DNS records for a domain the user owns on ShipStatic; a general question about DNS should be answered in prose without calling them.",
      "user_prompt": "Can you explain what a CNAME record is and how it differs from an A record?",
      "expected_output": "A plain explanation of the two record types. No ShipStatic tool is called and nothing is deployed."
    },
    {
      "description": "A hosting request the plugin cannot serve. ShipStatic hosts static sites only; a server process is out of scope, so the model should say so rather than deploy something that cannot run.",
      "user_prompt": "Deploy my Node.js Express API server with its PostgreSQL database so it runs 24/7.",
      "expected_output": "ChatGPT explains that ShipStatic hosts static sites (HTML, CSS, JavaScript and assets) and cannot run a server or a database, and suggests what would fit instead. No deploy is attempted and no card appears."
    }
  ]
}
